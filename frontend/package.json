{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "elm-land server",
    "build": "elm-land build && cp -r assets dist",
    "format": "elm-format --yes src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Strübing <mxstrbng@gmail-com> (https://github-com/mstruebing)",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "elm-land": "^0.20.1"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7"
  }
}
